#include <stdio.h>

void find_longest_increasing_subarray(int arr[], int size);

int main() {
    int arr[] = {1, 2, 2, 3, 5, 4, 7, 8};
    int size = sizeof(arr) / sizeof(arr[0]);

    find_longest_increasing_subarray(arr, size);

    return 0;
}

void find_longest_increasing_subarray(int arr[], int size) {
    // Kodunuzu buraya yazın
}
